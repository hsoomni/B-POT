# ── Django ──────────────────────────────────────────────
SECRET_KEY=change-me-to-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# ── 결과 생성 AI (선택) ─────────────────────────────────
# 키가 없으면 선택지 조립(fallback)만으로 완전히 동작합니다.
GEMINI_API_KEY=

# ── CTA → Google Sheets 웹훅 (선택) ─────────────────────
# Google Apps Script 웹앱 URL. 넣으면 문의가 시트로 전송됨.
SHEETS_WEBHOOK_URL=

# ── CTA 이메일 알림 (선택) ──────────────────────────────
# 미설정 시 콘솔 백엔드로 출력되어 로컬에서도 동작합니다.
EMAIL_TO=hsoomni@gmail.com
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=B-POT <no-reply@b-pot.com>
